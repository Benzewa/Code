#include<stdio.h>
#include<string.h>
int ft_str_is_alpha(char *str);
int main(void)
{
	return 0;
}
int ft_str_is_alpha(char *str)
{
	int i = 0;
	while(str[i] != '\0')
	{
		if((str[i] < 'a' || str[i] > 'z') && 
	}
}
